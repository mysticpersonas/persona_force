import LegalPage from '../components/LegalPage';
import { termsConditions } from '../data/legalContent';

const TermsConditions = () => <LegalPage doc={termsConditions} />;

export default TermsConditions;
